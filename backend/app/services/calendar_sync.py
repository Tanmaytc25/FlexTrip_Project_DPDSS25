# backend/app/services/calendar_sync.py

def sync_with_calendar(trip):
    # Placeholder: Integrate with Google Calendar API to create/update events
    print(f"Syncing trip '{trip.name}' with user's calendar.")
